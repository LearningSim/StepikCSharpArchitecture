namespace Inheritance.DataStructure {
	public enum MessageType
    {
        Incoming,
        Outgoing,
        Service
    }

    public enum MessageTopic
    {
        Subscribe,
        Error,
        Update
    }
}
